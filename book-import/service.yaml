apiVersion: v1
kind: Service
metadata:
  name: book-import2
  labels:
    name: book-import2
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    name: book-import